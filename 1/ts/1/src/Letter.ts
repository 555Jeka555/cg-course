class Letter {
    private static readonly INIT_Y = 100;
    private static readonly HEIGHT_OF_JUMP = 50;
    private static readonly ANIMATION_DURATION = 1000;

    constructor() {
    }

    draw (ctx: CanvasRenderingContext2D, currentDate: number): void {


    }
}